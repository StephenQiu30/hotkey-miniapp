services:
  api:
    env_file:
      - ./.env
    environment:
      APP_ENV: production
      DATABASE_URL: ${DATABASE_URL}

  web:
    env_file:
      - ./.env
    environment:
      APP_ENV: production
      NEXT_PUBLIC_API_BASE_URL: http://api:8000
    ports:
      - "80:3000"
