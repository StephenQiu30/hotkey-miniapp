# 16 企业级 P0 任务编排（基于 PRD 复核结果）

## 目标

基于已完成的 PRD 与 Plan，将企业级 P0 任务做统一编排，便于开发、验收和回滚管理。当前分三层：

1. 后端能力补齐（已完成）；
2. 运行级/环境级验收（待执行）；
3. 可观测与告警增强（待排期）。

## 任务拆解与进度

> 状态约定：✅ 已完成，🚧 进行中，📋 待执行，⏸️ 阻塞。

### 阶段 A：后端能力补齐（已完成）

| 任务ID | 来源 | 任务 | 依赖 | 验收 |
|---|---|---|---|---|
| A2-1 | PRD-06 / Plan-13 | Provider 异常策略配置与策略分支（fallback/skip/error） | 无 | settings 与策略行为可通过单测验证，`ai_provider_selection`/`fallback`/`skip` 日志存在 |
| A2-2 | PRD-06 / Plan-13 | AI 分析异常分支与查询扩展异常分支统一 | A2-1 | 主异常场景下可追溯处理、可复用测试证据 |
| A4-1 | PRD-07 / Plan-14 | 聚类元数据写入（`cluster_version`/`clustered_at`） | A4-0（历史去重链路） | 同一聚类能记录版本与时间 |
| A4-2 | PRD-07 / Plan-14 | 聚类回溯 API（cluster / cluster-history） | A4-1 | 接口返回 `cluster_id`、`cluster_size`、`items[]` |
| A8-1 | PRD-08 / Plan-15 | `User.role` + `require_permission` 最小权限模型 | 无 | `UserRead` 可返回 role，权限依赖可注入 |
| A8-2 | PRD-08 / Plan-15 | 关键写 API 接入权限 | A8-1 | 读写分离、受限场景返回 `403` |

完成后验收结论：  
- ✅ A2、A4、A8 相关能力已完成并已合并到主分支。
- 已补齐的代码、测试和文档见：
  - `docs/product/prd/06-LLM供应商切换与可观测性补齐PRD.md` + `docs/plans/13-LLM供应商切换与容错计划.md`
  - `docs/product/prd/07-热点聚类去重与版本化回溯PRD.md` + `docs/plans/14-热点聚类与回溯查询计划.md`
  - `docs/product/prd/08-安全增强-RBAC与权限治理PRD.md` + `docs/plans/15-安全增强与RBAC计划.md`

### 阶段 B：生产级运行和验收（待执行）

| 任务ID | 来源 | 任务 | 依赖 | 负责人 | 验收 |
|---|---|---|---|---|---|
| B1 | 运营验收 | 环境级运行验收：PostgreSQL 初始化、真实 X/Twitter/Bing/SMTP/模型场景回放 | 全部后端能力补齐 | （待分配） | 一套可追溯记录（日志/截图/命令）证明真实链路可跑 |
| B2 | 运营验收 | 部署链路与健康可达：API/Web/Nginx 联动、`/api/health`、`/app` 可访问 | A2/A4/A8 | （待分配） | compose 启动与访问证据 |
| B3 | 运营验收 | 监控/告警最小闭环：指标->告警策略（阈值）-> 演练记录 | B2，A2/A4/A8 的日志与指标项 | （待分配） | 告警触发测试 + 处理闭环记录 |

### 阶段 C：优化增强（待排期）

| 任务ID | 来源 | 任务 | 依赖 | 验收 |
|---|---|---|---|---|
| C1 | PRD-00 + A8 | 角色/权限边界治理文档化与运维手册补充 | A8-2 | 变更可追溯、管理员可按文档验证 |
| C2 | PRD-00 + A2/A4 | 告警阈值标准化与运行手册 | B3 | 告警策略文件、演练记录、SOP |

## 执行顺序（含批次）

- 批次 1（本轮收口后）：A2-1、A2-2、A4-1、A4-2、A8-1、A8-2（全部完成）
- 批次 2（本轮收口后）：B1、B2、B3（建议按“部署->回放->演练”执行）
- 批次 3（后续）：C1、C2（与运行观察指标对齐）

## 风险与回退

- 风险：环境级验收与生产链路演练与本地单测脱节。  
  - 回退：若真实链路不稳定，回滚到单测+本地 docker 联调状态先确保功能闭环完整，再分步补齐环境配置。
- 风险：告警策略先验不一致导致误报。  
  - 回退：先用保守阈值和临时人工确认流程，逐步收紧策略。
